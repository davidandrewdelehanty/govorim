{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Folder picker on first run; everything else is served locally.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
